class Solution(object):
    def lengthOfLastWord(self, s):
        """
        :type s: str
        :rtype: int
        """

        words = s.split()
        index = -1
        while True:
            if words[index] == ' ':
                index -= 1
            else:
                break
        return len(words[index])
